{% extends "template_base.html.twig" %}


{% block content %}
{# TODO : A voir comment récupérer les variables de session  #}
{% if app.session.get('username')  == 'Ervin' %}
    <div class="container">
        <form method="POST" action="?where=addPost">
            <input type="text" name="titre" placeholder="Titre" /><br />
            <textarea id="post" name="contenu" placeholder="Contenu de l'article"></textarea><br />
            <input type="submit" value="Poster"/>
        </form>
     </div>
{% else %}
    <p>Bonjour<p/>    
{% endif %}
{% endblock %}